{% macro project_card(title, technologies, description, image, link) %}
    <article class="main__card">
        <a class=main__card-link href="{{link}}">
            <img class="main__card-image {% if frame %} image_frame {% endif %} " src="{{image}}" alt="project photo">
            <h2 class="main__card-header">{{ title }}</h4>
            <h4 class="main__card-technologies">{{ technologies }}</h4>
            <p class="main__card-description"> {{ description }}</p>
        </a>
    </article>
{% endmacro%}